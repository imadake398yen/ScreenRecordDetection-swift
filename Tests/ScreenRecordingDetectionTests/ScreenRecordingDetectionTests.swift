import XCTest
@testable import ScreenRecordingDetection

final class ScreenRecordingDetectionTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ScreenRecordingDetection().text, "Hello, World!")
    }
}
